{
  "name": "cx-visual-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "fetch-catalog": "tsx tools/fetch-catalog.ts",
    "zip": "cd dist && zip -r ../cx-visual-extension.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.246",
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.0",
    "jsdom": "^24.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
